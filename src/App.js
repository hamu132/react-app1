import RotateIcon from './components/RotateIcon';
import SpeedButton from './components/SpeedButton';

//HTML要素が返される。
function App() {
  return (
    <div>
      <RotateIcon />
      <SpeedButton />
    </div>
  );
}

// Appコンポーネントをエクスポートする。

export default App;
